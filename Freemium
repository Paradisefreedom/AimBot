local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local RunService = game:GetService("RunService")
local TweenService = game:GetService("TweenService")

local player = Players.LocalPlayer
local camera = workspace.CurrentCamera
local playerGui = player:WaitForChild("PlayerGui")

-- Configuration & State
local state = {
    AimEnabled = false,
    TargetLock = false,
    AutoShoot = false,
    ShowFOV = true,
    FOV = 150,
    Smoothness = 0.2,
    AimKeybind = Enum.KeyCode.E,
    MenuKeybind = Enum.KeyCode.RightShift,
    IsBinding = false
}

local aiming = false
local currentTarget = nil
local lastShootTime = 0
local shootCooldown = 0.1 -- Adjust for fire rate (0.1 = 10 clicks per sec)

-- Accent Color Theme (Electric Blue)
local accentColor = Color3.fromRGB(0, 120, 255)
local bgDark = Color3.fromRGB(20, 20, 20)
local bgLight = Color3.fromRGB(35, 35, 35)
local textCol = Color3.fromRGB(240, 240, 240)
local textDim = Color3.fromRGB(150, 150, 150)

-- --- UI CONSTRUCTION --- --
local screenGui = Instance.new("ScreenGui")
screenGui.Name = "ModernTestingUI"
screenGui.ResetOnSpawn = false
screenGui.Parent = playerGui

-- FOV Circle Visualizer
local fovCircle = Instance.new("Frame")
fovCircle.Name = "FOVCircle"
fovCircle.AnchorPoint = Vector2.new(0.5, 0.5)
fovCircle.BackgroundTransparency = 1 -- Hide fill
fovCircle.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
fovCircle.BorderSizePixel = 0
fovCircle.Visible = state.ShowFOV
fovCircle.Parent = screenGui

local fovStroke = Instance.new("UIStroke")
fovStroke.Color = accentColor
fovStroke.Thickness = 1.5
fovStroke.Transparency = 0.5
fovStroke.Parent = fovCircle

local fovCorner = Instance.new("UICorner")
fovCorner.CornerRadius = UDim.new(1, 0)
fovCorner.Parent = fovCircle

-- Main Menu Frame
local mainFrame = Instance.new("Frame")
mainFrame.Name = "MainFrame"
mainFrame.Size = UDim2.new(0, 280, 0, 420)
mainFrame.Position = UDim2.new(0.5, -140, 0.5, -210)
mainFrame.BackgroundColor3 = bgDark
mainFrame.BorderSizePixel = 0
mainFrame.Parent = screenGui
mainFrame.Active = true
mainFrame.Draggable = true
mainFrame.ClipsDescendants = true

local dropShadow = Instance.new("UIStroke")
dropShadow.Color = Color3.fromRGB(0,0,0)
dropShadow.Thickness = 2
dropShadow.Transparency = 0.7
dropShadow.Parent = mainFrame

local mainCorner = Instance.new("UICorner")
mainCorner.CornerRadius = UDim.new(0, 8)
mainCorner.Parent = mainFrame

-- Header
local header = Instance.new("Frame")
header.Size = UDim2.new(1, 0, 0, 40)
header.BackgroundColor3 = bgLight
header.BorderSizePixel = 0
header.Parent = mainFrame

local headerCorner = Instance.new("UICorner")
headerCorner.CornerRadius = UDim.new(0, 8)
headerCorner.Parent = header

-- Fix bottom corners of header to blend with main body
local headerFix = Instance.new("Frame")
headerFix.Size = UDim2.new(1, 0, 0, 10)
headerFix.Position = UDim2.new(0, 0, 1, -10)
headerFix.BackgroundColor3 = bgLight
headerFix.BorderSizePixel = 0
headerFix.Parent = header

local title = Instance.new("TextLabel")
title.Size = UDim2.new(1, -30, 1, 0)
title.Position = UDim2.new(0, 15, 0, 0)
title.BackgroundTransparency = 1
title.Text = "MODERN ENV"
title.TextColor3 = accentColor
title.Font = Enum.Font.GothamBold
title.TextSize = 14
title.TextXAlignment = Enum.TextXAlignment.Left
title.Parent = header

-- Scroll Frame for Content
local contentLayout = Instance.new("ScrollingFrame")
contentLayout.Size = UDim2.new(1, 0, 1, -50)
contentLayout.Position = UDim2.new(0, 0, 0, 50)
contentLayout.BackgroundTransparency = 1
contentLayout.ScrollBarThickness = 2
contentLayout.ScrollBarImageColor3 = accentColor
contentLayout.CanvasSize = UDim2.new(0, 0, 0, 0)
contentLayout.AutomaticCanvasSize = Enum.AutomaticSize.Y
contentLayout.Parent = mainFrame

local listLayout = Instance.new("UIListLayout")
listLayout.Parent = contentLayout
listLayout.SortOrder = Enum.SortOrder.LayoutOrder
listLayout.Padding = UDim.new(0, 8)
listLayout.HorizontalAlignment = Enum.HorizontalAlignment.Center

local padding = Instance.new("UIPadding")
padding.Parent = contentLayout
padding.PaddingTop = UDim.new(0, 5)
padding.PaddingBottom = UDim.new(0, 15)

-- --- UI COMPONENTS --- --

-- Animated Toggle Switch
local function createToggle(name, stateKey, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 250, 0, 35)
    frame.BackgroundColor3 = bgLight
    frame.Parent = contentLayout
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 6)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.7, 0, 1, 0)
    label.Position = UDim2.new(0, 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = textCol
    label.Font = Enum.Font.GothamSemibold
    label.TextSize = 13
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local toggleBg = Instance.new("TextButton")
    toggleBg.Size = UDim2.new(0, 40, 0, 20)
    toggleBg.Position = UDim2.new(1, -50, 0.5, -10)
    toggleBg.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    toggleBg.Text = ""
    toggleBg.AutoButtonColor = false
    toggleBg.Parent = frame
    Instance.new("UICorner", toggleBg).CornerRadius = UDim.new(1, 0)

    local knob = Instance.new("Frame")
    knob.Size = UDim2.new(0, 16, 0, 16)
    knob.Position = UDim2.new(0, 2, 0.5, -8)
    knob.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
    knob.Parent = toggleBg
    Instance.new("UICorner", knob).CornerRadius = UDim.new(1, 0)

    local function updateVisuals(isSetup)
        local targetColor = state[stateKey] and accentColor or Color3.fromRGB(60, 60, 60)
        local targetPos = state[stateKey] and UDim2.new(1, -18, 0.5, -8) or UDim2.new(0, 2, 0.5, -8)
        
        if isSetup then
            toggleBg.BackgroundColor3 = targetColor
            knob.Position = targetPos
        else
            TweenService:Create(toggleBg, TweenInfo.new(0.2), {BackgroundColor3 = targetColor}):Play()
            TweenService:Create(knob, TweenInfo.new(0.2, Enum.EasingStyle.Back), {Position = targetPos}):Play()
        end
    end
    updateVisuals(true)

    toggleBg.MouseButton1Click:Connect(function()
        state[stateKey] = not state[stateKey]
        updateVisuals(false)
        if callback then callback(state[stateKey]) end
    end)
end

-- Draggable Slider Component
local function createSlider(name, stateKey, min, max, isFloat, callback)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 250, 0, 50)
    frame.BackgroundColor3 = bgLight
    frame.Parent = contentLayout
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 6)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(1, -20, 0, 20)
    label.Position = UDim2.new(0, 10, 0, 5)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = textCol
    label.Font = Enum.Font.GothamSemibold
    label.TextSize = 13
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local valueLabel = Instance.new("TextLabel")
    valueLabel.Size = UDim2.new(0, 50, 0, 20)
    valueLabel.Position = UDim2.new(1, -60, 0, 5)
    valueLabel.BackgroundTransparency = 1
    valueLabel.Text = tostring(state[stateKey])
    valueLabel.TextColor3 = accentColor
    valueLabel.Font = Enum.Font.GothamBold
    valueLabel.TextSize = 13
    valueLabel.TextXAlignment = Enum.TextXAlignment.Right
    valueLabel.Parent = frame

    local sliderBg = Instance.new("TextButton")
    sliderBg.Size = UDim2.new(1, -20, 0, 6)
    sliderBg.Position = UDim2.new(0, 10, 1, -15)
    sliderBg.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
    sliderBg.Text = ""
    sliderBg.AutoButtonColor = false
    sliderBg.Parent = frame
    Instance.new("UICorner", sliderBg).CornerRadius = UDim.new(1, 0)

    local fill = Instance.new("Frame")
    fill.Size = UDim2.new(0, 0, 1, 0) -- Set dynamically
    fill.BackgroundColor3 = accentColor
    fill.Parent = sliderBg
    Instance.new("UICorner", fill).CornerRadius = UDim.new(1, 0)

    -- Initial fill size
    local percent = (state[stateKey] - min) / (max - min)
    fill.Size = UDim2.new(percent, 0, 1, 0)

    local dragging = false

    local function updateSlider(input)
        local pos = math.clamp((input.Position.X - sliderBg.AbsolutePosition.X) / sliderBg.AbsoluteSize.X, 0, 1)
        fill.Size = UDim2.new(pos, 0, 1, 0)
        
        local val = min + ((max - min) * pos)
        if not isFloat then val = math.floor(val) else val = math.floor(val * 100) / 100 end
        
        state[stateKey] = val
        valueLabel.Text = tostring(val)
        if callback then callback(val) end
    end

    sliderBg.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            updateSlider(input)
        end
    end)

    UserInputService.InputEnded:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = false
        end
    end)

    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            updateSlider(input)
        end
    end)
end

-- Keybind Component
local function createKeybind(name, stateKey)
    local frame = Instance.new("Frame")
    frame.Size = UDim2.new(0, 250, 0, 35)
    frame.BackgroundColor3 = bgLight
    frame.Parent = contentLayout
    Instance.new("UICorner", frame).CornerRadius = UDim.new(0, 6)

    local label = Instance.new("TextLabel")
    label.Size = UDim2.new(0.6, 0, 1, 0)
    label.Position = UDim2.new(0, 10, 0, 0)
    label.BackgroundTransparency = 1
    label.Text = name
    label.TextColor3 = textCol
    label.Font = Enum.Font.GothamSemibold
    label.TextSize = 13
    label.TextXAlignment = Enum.TextXAlignment.Left
    label.Parent = frame

    local bindBtn = Instance.new("TextButton")
    bindBtn.Size = UDim2.new(0, 80, 0, 25)
    bindBtn.Position = UDim2.new(1, -90, 0.5, -12.5)
    bindBtn.BackgroundColor3 = bgDark
    bindBtn.Text = state[stateKey].Name
    bindBtn.TextColor3 = accentColor
    bindBtn.Font = Enum.Font.GothamBold
    bindBtn.TextSize = 12
    bindBtn.AutoButtonColor = false
    bindBtn.Parent = frame
    Instance.new("UICorner", bindBtn).CornerRadius = UDim.new(0, 4)

    bindBtn.MouseButton1Click:Connect(function()
        state.IsBinding = true
        bindBtn.Text = "..."
        bindBtn.TextColor3 = Color3.fromRGB(255, 200, 0)
    end)

    UserInputService.InputBegan:Connect(function(input, gameProcessed)
        if state.IsBinding and input.UserInputType == Enum.UserInputType.Keyboard then
            state[stateKey] = input.KeyCode
            state.IsBinding = false
            bindBtn.Text = state[stateKey].Name
            bindBtn.TextColor3 = accentColor
        end
    end)
end

-- Initialize the specific UI items
createToggle("Main Aim Toggle", "AimEnabled")
createToggle("Target Lock", "TargetLock")
createToggle("Auto Shoot", "AutoShoot")
createToggle("Show FOV Circle", "ShowFOV", function(val) fovCircle.Visible = val end)
createSlider("FOV Radius", "FOV", 10, 500, false, function(val) fovCircle.Size = UDim2.new(0, val*2, 0, val*2) end)
createSlider("Aim Smoothness", "Smoothness", 0.01, 1, true)
createKeybind("Aim Keybind", "AimKeybind")


-- --- AIMBOT & AUTO SHOOT LOGIC --- --

local function getClosestPlayerInFOV()
    local mousePos = UserInputService:GetMouseLocation()
    local closestDist = state.FOV
    local closestPlr = nil

    for _, targetPlr in ipairs(Players:GetPlayers()) do
        if targetPlr ~= player and targetPlr.Character and targetPlr.Character:FindFirstChild("HumanoidRootPart") and targetPlr.Character:FindFirstChild("Humanoid") then
            if targetPlr.Character.Humanoid.Health > 0 then
                local hrp = targetPlr.Character.HumanoidRootPart
                local screenPos, onScreen = camera:WorldToViewportPoint(hrp.Position)

                if onScreen then
                    local dist = (Vector2.new(screenPos.X, screenPos.Y) - mousePos).Magnitude
                    if dist <= state.FOV then
                        if dist < closestDist then
                            closestDist = dist
                            closestPlr = targetPlr
                        end
                    end
                end
            end
        end
    end
    return closestPlr
end

-- Trigger Aim via Keybind
UserInputService.InputBegan:Connect(function(input, gameProcessed)
    if state.IsBinding then return end

    -- Hide/Show UI Menu Keybind
    if input.KeyCode == state.MenuKeybind then
        mainFrame.Visible = not mainFrame.Visible
    end

    if not gameProcessed then
        if input.KeyCode == state.AimKeybind and state.AimEnabled then
            aiming = true
            if state.TargetLock then
                currentTarget = getClosestPlayerInFOV()
            end
        end
    end
end)

UserInputService.InputEnded:Connect(function(input, gameProcessed)
    if not gameProcessed and input.KeyCode == state.AimKeybind then
        aiming = false
        currentTarget = nil
    end
end)

-- Main Loop
RunService.RenderStepped:Connect(function()
    -- Dynamic FOV Circle update
    if state.AimEnabled and state.ShowFOV then
        local mousePos = UserInputService:GetMouseLocation()
        fovCircle.Position = UDim2.new(0, mousePos.X, 0, mousePos.Y)
    end

    if state.AimEnabled and aiming then
        local targetToAimAt = nil

        if state.TargetLock then
            if currentTarget and currentTarget.Character and currentTarget.Character:FindFirstChild("HumanoidRootPart") and currentTarget.Character.Humanoid.Health > 0 then
                targetToAimAt = currentTarget
            else
                currentTarget = getClosestPlayerInFOV()
                targetToAimAt = currentTarget
            end
        else
            targetToAimAt = getClosestPlayerInFOV()
        end

        if targetToAimAt then
            local targetHRP = targetToAimAt.Character.HumanoidRootPart
            local targetCameraCFrame = CFrame.new(camera.CFrame.Position, targetHRP.Position)
            local smoothFactor = math.clamp(state.Smoothness, 0.01, 1) 
            
            camera.CFrame = camera.CFrame:Lerp(targetCameraCFrame, smoothFactor)

            -- Auto Shoot Logic execution
            if state.AutoShoot then
                if tick() - lastShootTime >= shootCooldown then
                    lastShootTime = tick()
                    
                    -- If testing in standard executor environment:
                    if mouse1click then
                        mouse1click()
                    else
                        -- Safe Fallback for Roblox Studio standard testing:
                        -- Automatically activates whatever tool your character is holding
                        if player.Character then
                            local tool = player.Character:FindFirstChildOfClass("Tool")
                            if tool then
                                tool:Activate()
                            end
                        end
                    end
                end
            end
        end
    end
end)
